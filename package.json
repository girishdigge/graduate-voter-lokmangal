{
  "name": "voter-management-system",
  "version": "1.0.0",
  "description": "Padhvidhar Matdar Sangh - Voter Management System",
  "private": true,
  "workspaces": [
    "frontend-public",
    "frontend-admin",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:public\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:public": "npm run dev --workspace=frontend-public",
    "dev:admin": "npm run dev --workspace=frontend-admin",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces",
    "install:all": "npm install && npm install --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
